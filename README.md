# Module used to manage a statically allocated pool structure
This module can be used to make any data type poolable.

## Build this module standalone for testing
mkdir build  
cd build  
cmake .. -DSTATIC_POOL_TEST=ON  
make  